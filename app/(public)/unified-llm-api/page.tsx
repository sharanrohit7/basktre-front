import type { Metadata } from "next";
import { SeoPage } from "@/components/seo/SeoPage";
export const metadata: Metadata = { title: "Unified LLM API for GPT, Claude, Gemini & More", description: "Access multiple LLM providers through one API key and consistent messages interface. Switch models or use automatic routing without separate provider integrations.", alternates: { canonical: "/unified-llm-api" } };
export default function Page() { return <SeoPage path="/unified-llm-api" eyebrow="// one integration" title="One unified LLM API for 60+ models" intro="Connect once, then use GPT, Claude, Gemini, Llama, DeepSeek and more with a single API key and consistent request format."><h2>Replace provider-specific plumbing</h2><ul><li>One API key and wallet across supported providers.</li><li>One messages-based request shape for model calls.</li><li>Streaming and non-streaming endpoints.</li><li>Manual model tags or automatic routing.</li><li>Transparent input and output token prices.</li></ul><h2>Switch models in one field</h2><pre>{JSON.stringify({ model: "claude-haiku-4-5", messages: [{ role: "user", content: "Hello" }] }, null, 2)}</pre><p>Change the model tag to test another supported model, or use <code>auto</code> when cost-aware routing fits your workload. Confirm current features in the <a className="underline" href="/docs">API documentation</a>.</p></SeoPage>; }
