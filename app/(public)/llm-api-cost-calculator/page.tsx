import type { Metadata } from "next"; import CostCalculator from "@/components/pricing/CostCalculator"; import { SeoPage } from "@/components/seo/SeoPage"; import { getProviders } from "@/lib/server/pricing";
export const metadata:Metadata={title:"LLM API Cost Calculator — Compare Model Pricing",description:"Estimate and compare GPT, Claude, Gemini, Llama and DeepSeek API costs using current input and output token prices.",alternates:{canonical:"/llm-api-cost-calculator"}};
export default async function Page(){const providers=await getProviders();return <SeoPage path="/llm-api-cost-calculator" eyebrow="// cost calculator" title="Estimate your LLM API costs" intro="Enter your average input tokens, output tokens and request volume to compare selected models using current per-million-token rates."><CostCalculator providers={providers}/><h2>How the estimate works</h2><p>For every model, the calculator multiplies input and output tokens by their respective rates, then multiplies that cost by request volume. The funding estimate adds Basktre’s 4% wallet top-up fee. Prices can change, so treat results as estimates.</p></SeoPage>}
