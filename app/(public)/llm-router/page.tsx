import type { Metadata } from "next";
import Link from "next/link";
import { SeoPage } from "@/components/seo/SeoPage";
export const metadata: Metadata = { title: "Intelligent LLM Router for Cost-Efficient AI APIs", description: "Route AI requests to cost-effective models automatically or select GPT, Claude, Gemini, Llama and DeepSeek manually through one API.", alternates: { canonical: "/llm-router" } };
const faqs = [{ question: "What is an LLM router?", answer: "An LLM router selects a model for each request using defined signals such as task needs and cost, instead of sending every request to one model." }, { question: "Can I override automatic routing?", answer: "Yes. Send a specific supported model tag whenever you need deterministic model selection." }];
export default function Page() { return <SeoPage path="/llm-router" eyebrow="// intelligent routing" title="An LLM router built to lower API cost" intro="Use model: auto to route requests to a cost-effective model that can handle the task, or specify a model whenever you need full control." faqs={faqs}><h2>How automatic LLM routing works</h2><ol><li>Your application sends a normal messages request.</li><li>The router evaluates the request and selects a capable model.</li><li>Basktre forwards the request without storing its prompt or response body.</li><li>The response reports which model ran and the associated usage.</li></ol><pre>{JSON.stringify({ model: "auto", messages: [{ role: "user", content: "Summarize this text" }] }, null, 2)}</pre><h2>Automatic when useful, manual when required</h2><p>Routing is intended for workloads where cost flexibility matters. Pin a model for benchmark consistency, provider-specific behavior or capabilities your application depends on.</p><p>Compare current rates on the <Link className="underline" href="/llm-api-pricing">LLM API pricing page</Link>.</p></SeoPage>; }
