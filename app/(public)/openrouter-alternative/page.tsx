import type { Metadata } from "next";
import { SeoPage } from "@/components/seo/SeoPage";
export const metadata: Metadata = { title: "OpenRouter Alternative for Lower LLM API Costs", description: "Compare Basktre as an OpenRouter alternative with automatic cost routing, direct model rates, a 4% wallet top-up fee, and no prompt or response logging.", alternates: { canonical: "/openrouter-alternative" } };
const faqs = [{ question: "Does Basktre store prompts or model responses?", answer: "No. Basktre does not store prompt or response bodies. It retains limited billing metadata such as model, token counts, cost, status and timestamp." }, { question: "Can I choose a model manually?", answer: "Yes. Specify a model tag for direct control or use model: auto for automatic cost-aware routing." }];
export default function Page() { return <SeoPage path="/openrouter-alternative" eyebrow="// openrouter alternative" title="A cost-saving OpenRouter alternative for developers" intro="Basktre gives you one key for 60+ models, automatic cost-aware routing, direct model rates, and an architecture that does not log prompt or response bodies." faqs={faqs}><h2>Compare the approach</h2><table><thead><tr><th>Capability</th><th>Basktre</th><th>What to verify elsewhere</th></tr></thead><tbody><tr><td>Pricing</td><td>4% fee on wallet top-ups; no per-token markup</td><td>Current funding and platform fees</td></tr><tr><td>Routing</td><td>Cheapest-capable automatic routing or manual selection</td><td>Available routing policies</td></tr><tr><td>Content logging</td><td>No prompt or response bodies stored</td><td>Provider-specific retention controls</td></tr><tr><td>Trial</td><td>$1 credit on signup, no card</td><td>Current introductory offer</td></tr></tbody></table><h2>Migrate without rewriting your application</h2><p>Create a Basktre key, replace your gateway endpoint and credentials, then choose a model tag or set <code>model: &quot;auto&quot;</code>. Test streaming, tool calls and error handling before switching production traffic.</p><pre>curl https://api.basktre.in/api/v1/models/stream -H &quot;api-key: your_basktre_key&quot;</pre><h2>A factual comparison, not a universal verdict</h2><p>Reliability, model coverage and feature requirements differ by application. Compare current documentation and pricing before migrating; this page deliberately avoids claims about another service’s logging or changeable fees.</p></SeoPage>; }
